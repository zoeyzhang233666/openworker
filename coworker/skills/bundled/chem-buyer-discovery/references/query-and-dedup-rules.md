# 查询学习与主体去重

查询学习只接受可回溯来源；低质量目录不能污染语言地图。目标市场的排除条件作用于每一轮查询，而不是全局永久排除贸易商。

发现阶段允许 manufacturer、formulator、importer、distributor 等角色进入候选池；它们是否符合 ICP 由核验阶段决定。没有官网或登记信息的候选保留为 `Discovered`，不要猜测其法律主体。每个候选必须保留发现证据与产生它的查询 ID；重复组记录合并依据和证据，无法安全合并的配对放入 `unresolved_pairs`。
