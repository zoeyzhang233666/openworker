# 钩子 / CTA 反模式

- **不得新增**原文与 `RewriteBrief` 没有的 CAS、纯度、等级、包装、认证、检测结论、客户案例、价格、交期、市场排名。
- 禁止为「更吸睛」改写数字或 CAS。
- 禁止疗效、安全绝对化、包过类表述（即使正文已清掉，钩子也不得加回）。
- 禁止把钩子包当成洗稿降重工具；独立表达仍以事实行为准。
- 钩子写入成稿后必须重新跑 `chem-content-policy` 与 `chem-content-quality-check`；本包不做最终 pass。
